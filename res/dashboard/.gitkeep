Drop the built sing-box dashboard here (the contents of the gh-pages archive,
with index.html at the top level). CMake embeds whatever it finds into the
Quattro binary; leave it empty to fall back to downloading on first use.
